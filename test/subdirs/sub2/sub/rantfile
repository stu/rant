
file "rootdep.t" do |t|
    sys.touch t.name
end

file "rootref.t" => "#t" do |t|
    test(?f, "../../t") || t.fail
    sys.touch t.name
end

task :clean do
    sys.rm_f Dir["*t"]
end

# vim:ft=ruby
